{
    "configurations": [
    {
        "command": "npm start | awk -v UI_NO=_dev '{printf "dokimion_ui_"UI_NO; print $0}' | logger -p user.info",
        "name": "Run npm start",
        "request": "launch",
        "type": "chrome",
         "url":  "http://192.168.56.107",
        "skipFiles": [
            "<node_internals>/**"
        ],
        "env": {
            "NODE_OPTIONS": "--openssl-legacy-provider",
        },

    }
    ]
}
